"""Source / parser layer.

WP1 owns only the pure helpers at the top of ``_common.py`` (jsonl iteration,
text flattening, tool categorization, usage de-cumulation, and the Session
builder). The raw per-format decoders and ``detect.py`` are added by WP2.
"""
